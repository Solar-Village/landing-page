# Roadmap Todos

These are all roadmap items. This file should contain unstarted todos grouped under thematic headings, plus any carried-over active work under `Ongoing`. At the end of a session, move the active todo card either into the daily session log if it is complete or into `Ongoing` if work remains.

## Ongoing

No carried-over active items.

## CI And Tooling

- CI Baseline: Add GitHub Actions for install, lint, test, and build using the chosen package manager.
- Package Manager Cleanup: Set this repo to pnpm, remove conflicting lockfiles, and document the policy.
